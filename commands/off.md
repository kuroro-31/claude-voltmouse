---
description: Restore the status line voltmouse replaced
allowed-tools: Bash(bash:*)
---

Run `bash "${CLAUDE_PLUGIN_ROOT}/scripts/toggle.sh" off`, then confirm which
status line is now in effect (the previous one, or none).
