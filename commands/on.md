---
description: Enable the voltmouse status line
allowed-tools: Bash(bash:*)
---

Run `bash "${CLAUDE_PLUGIN_ROOT}/scripts/toggle.sh" on`, then tell the user the
status line is active and that it appears on the next render.
